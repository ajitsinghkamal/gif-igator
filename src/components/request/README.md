### REQUEST Component
___ 

This is an HOC using render children to make a network call. Created for centralizing api calls since the response and call structure for each of the feature is same across app.